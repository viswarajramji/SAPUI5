<mvc:View
    controllerName="test.controller.Secondary"
    xmlns:mvc="sap.ui.core.mvc"
    displayBlock="true"
    xmlns="sap.m"
>

    <Panel headerText="{i18n>panelHeader}">
        <content>
            <Button text="press me" press=".press"></Button>
            <Input id="username" value="{/user/name}" description="hello {/user/name}" width="60%"></Input>
        </content>
    </Panel>
</mvc:View>