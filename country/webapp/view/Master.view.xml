<mvc:View xmlns:control="sap.gantt.control" xmlns:f="sap.f"
	controllerName="country.controller.Master"
	xmlns:mvc="sap.ui.core.mvc"
	xmlns="sap.m"
	xmlns:c="sap.ui.core"
>
<f:DynamicPage>
    <f:title>
        <f:DynamicPageTitle >
            <f:heading>
                <Title text="Products ({products>/ProductCollectionStats/Counts/Total})"/>
            </f:heading>
        </f:DynamicPageTitle>
    </f:title>
    <f:content>
        <VBox>
            <OverflowToolbar >
                <ToolbarSpacer/>
                    <SearchField width="30%" search=".filter"> </SearchField>
                    <OverflowToolbarButton icon="sap-icon://add" text="Add" type="Transparent" press=".onAdd"/>
					<OverflowToolbarButton icon="sap-icon://sort" text="Sort" type="Transparent" press=".onSort"/>
            </OverflowToolbar>
            <Table id="tableList" alternateRowColors="true"  items="{
                path:'products>/ProductCollection',
                sorter: {
                    path: 'Name'
                }}">
                <columns>
                    <Column>
                        <Text text="Product"></Text>
                    </Column>
                    <Column hAlign="Center">
                        <Text text="Price"></Text>
                    </Column>
                </columns>
                <items>
                    <ColumnListItem type="Navigation">
                        <cells>
                            <ObjectIdentifier title="{products>Name}" text="{products>ProductId}"/>
                                <ObjectNumber
                                number="{
                                    parts:[
                                        {path:'products>Price'},
                                        {path:'products>CurrencyCode'}
                                    ],
                                    type: 'sap.ui.model.type.Currency',
                                    formatOptions: {showMeasure: false}
                                }"
                                unit="{products>CurrencyCode}"/>
                        </cells>
                    </ColumnListItem>
                </items>
            </Table>
        </VBox>
    </f:content>
	<!-- DynamicPage Footer -->
    <!-- <f:footer >
        <OverflowToolbar >
            <ToolbarSpacer/>
            <Button type="Accept" text="Accept" icon="sap-icon://save"/>
            <Button type="Reject" text="Reject"/>
        </OverflowToolbar>
    </f:footer> -->
    <f:header>
    </f:header>
   
</f:DynamicPage>
</mvc:View>